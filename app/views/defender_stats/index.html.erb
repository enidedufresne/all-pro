<div class="row">
<div class="col-lg-12 well">
<p id="notice"><%= notice %></p>

<h1>Listing Defender Stats</h1>

<div class="table-responsive">
<table class="table">
  <thead>
    <tr>
      <th>Event</th>
      <th>Win</th>
      <th>Draw</th>
      <th>Score</th>
      <th>Goals</th>
      <th>Assists</th>
      <th>Shots taken</th>
      <th>Athlete</th>
    </tr>
  </thead>

  <tbody>
    <% @defender_stats.each do |defender_stat| %>
      <tr>
        <td><%= defender_stat.event %></td>
        <td><%= defender_stat.win ? 'Yes' : 'No' %></td>
        <td><%= defender_stat.draw ? 'Yes' : 'No' %></td>
        <td><%= defender_stat.score %> - <%= defender_stat.opponent_score %></td>
        <td><%= defender_stat.goals %></td>
        <td><%= defender_stat.assists %></td>
        <td><%= defender_stat.shots_taken %></td>
        <td><%= defender_stat.user.try(:name) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>