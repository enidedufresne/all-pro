<div id="event-<%= @forward_stat.id %>" class="event">
  <div class="row">
    <div class="col-lg-6 col-lg-offset-3 well">
      <%= render @forward_stat, remote: false %>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-6 col-lg-offset-3 well">
      <h4>Comments</h4>
      <hr>
      <%= render 'comments/form', commentable: @forward_stat %>
      <br>
      <div class="comments">
        <%= render(@comments) || render('shared/no_resource', resources: 'Comments') %>
      </div>
    </div>
  </div>
</div>


<p>
  <strong>Event:</strong>
  <%= @forward_stat.event %>
</p>


<p>
  <strong>Win:</strong>
  <%= @forward_stat.win %>
</p>

<p>
  <strong>Draw:</strong>
  <%= @forward_stat.draw %>
</p>

<p>
  <strong>Goals:</strong>
  <%= @forward_stat.goals %>
</p>

<p>
  <strong>Assists:</strong>
  <%= @forward_stat.assists %>
</p>

<p>
  <strong>Shots taken:</strong>
  <%= @forward_stat.shots_taken %>
</p>
