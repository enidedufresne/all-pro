<div class="row">
<div class="col-lg-12 well">
<p id="notice"><%= notice %></p>

<h1>Listing Forward Stats</h1>

<div class="table-responsive">
<table class="table">
  <thead>
    <tr>
      <th>Event</th>
      <th>Win</th>
      <th>Draw</th>
      <th>Score</th>
      <th>Goals</th>
      <th>Assists</th>
      <th>Shots taken</th>
      <th>Athlete</th>
    </tr>
  </thead>

  <tbody>
    <% @forward_stats.each do |forward_stat| %>
      <tr>
        <td><%= forward_stat.event %></td>
        <td><%= forward_stat.win ? 'Yes' : 'No' %></td>
        <td><%= forward_stat.draw ? 'Yes' : 'No' %></td>
        <td><%= forward_stat.score %> - <%= forward_stat.opponent_score %></td>
        <td><%= forward_stat.goals %></td>
        <td><%= forward_stat.assists %></td>
        <td><%= forward_stat.shots_taken %></td>
        <td><%= forward_stat.user.try(:name) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>